#include <stdio.h>
int main()
{
    //Here we declear the age variable.
    int age;
    printf("Enter your age\n");
    scanf("%d", &age);
    //next we declear the age input.
    printf("You have enter your age %d\n", age);
    if (age >= 18)
    {
        printf("Congratulations now you can watch porn");
    }
    else if (age > 12)
    {
        printf("I preffer you for musterbation");
    }

    else
    {
        printf("Sorry!you cannot watch porn");
    }

    return 0;
}
// its mainly use for different condition and diffent states and if else is use for determine for this steps