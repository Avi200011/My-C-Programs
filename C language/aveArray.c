#include<stdio.h>
int main()
{
    int marks[4],sum=0;
    printf("Enter the number of the 4 students:\n");
    for (int i = 0; i <=9; i++)
    {
        printf("Marks of the student %d");
        scanf("%d",&marks[i]);
        sum += marks[i]
    }
    
}